[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --filter @workspace/discord-bot"]

[phases.build]
cmds = []

[start]
cmd = "pnpm --filter @workspace/discord-bot run start:prod"
