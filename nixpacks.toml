[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npx pnpm@10 install --filter @workspace/discord-bot"
]

[phases.build]
cmds = []

[start]
cmd = "npx pnpm@10 --filter @workspace/discord-bot run start:prod"
